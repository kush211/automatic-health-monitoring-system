
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARILY OPEN for initial data seeding.
    // TODO: Secure these rules after authentication is implemented.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
